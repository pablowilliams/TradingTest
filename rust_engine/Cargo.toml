[package]
name = "trading_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "trading_engine"
crate-type = ["cdylib"]

# #44: Removed unused tokio, reqwest, serde deps - only pyo3 is needed
[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
